import {
  getSceneIndex,
  getNextSceneKey,
  BASE_TOTAL_DURATION_SECONDS,
} from "./SceneRegistry";

export const getActDuration = () => BASE_TOTAL_DURATION_SECONDS;
export { getSceneIndex, getNextSceneKey };
